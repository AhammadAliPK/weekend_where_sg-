[build]
command = "pnpm install && pnpm --filter @weekend-where-sg/database build && pnpm --filter api build"

[deploy]
startCommand = "node apps/api/dist/index.js"
healthcheckPath = "/api/health"

[env]
NODE_ENV = "production"